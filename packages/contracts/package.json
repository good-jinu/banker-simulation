{
  "name": "@banker-simulation/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Versioned contract AST, validation, summaries, projections, and compilation.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm test"
  },
  "dependencies": {
    "@banker-simulation/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2"
  }
}
