# 대형 시장 지도와 포트폴리오 리스크 기반 만들기

## 배경

`Banker Simulation`의 현재 핵심 루프는 고객 대화, 신용 판단, 대출 실행,
상환 및 부도 결과, 유동성 관리로 구성되어 있다. 고객에는 이미 업종
세그먼트가 있고, 시장 뉴스는 세그먼트별 위험을 조정한다. 그러나 현재
시스템은 소수 고객을 중앙 은행에 직접 연결해 표시하는 구조라서 다음의
전략적 선택을 충분히 만들지 못한다.

- 한 고객의 부도가 같은 업종 또는 지역의 다른 대출에 미치는 영향
- 시간이 지날수록 커지는 신청 규모, 건수, 업종 집중도
- 여러 지역과 업종에 분산하는 포트폴리오 운영

화면의 3D 도시는 이미 Three.js로 렌더링되지만, 현재는 배경 연출이다.
고객 노드의 2D 좌표, 도시 건물의 3D 좌표, 게임의 세그먼트 데이터가
서로 연결되지 않는다. 고객과 예금자에게 쓸 수 있는 위치도 고정된
10개뿐이고, 연결선은 모두 은행 허브에서 뻗는 단순한 별 모양이다.

따라서 대출·노드 수만 늘리거나 도시 격자만 넓히는 방식은 노드 겹침,
연결선 혼잡, 성능 저하를 만들 뿐 게임 판단을 깊게 만들지 못한다.
먼저 대형 지도에 맞는 데이터와 표현 구조를 마련한 뒤, 상관 리스크와
라운드 성장 규칙을 연결한다.

## 목표

1. 지역, 필지, 고객·상품·자금조달 노드, 연결 관계를 표현하는 지도
   데이터 모델을 만든다.
2. 2D 상호작용 노드와 3D 도시가 동일한 지역·좌표 데이터를 사용하게
   만든다.
3. 확대 수준에 따라 지역 요약, 업종 클러스터, 개별 고객을 구분해
   표시하여 많은 노드를 읽을 수 있게 한다.
4. 업종·지역 단위의 부도 전이와 라운드별 수요 성장을 게임 코어에
   안전하게 연결할 기반을 만든다.
5. 기존의 대화 중심 신용 심사와 결정적 순수 reducer 구조를 유지한다.

## 기대 효과

- **포트폴리오 판단**: 개별 고객의 위험뿐 아니라 이미 보유한 업종·지역
  노출을 고려해 승인 여부를 결정하게 된다.
- **분산의 의미**: 같은 업종 또는 지역에 대출을 집중했을 때 연쇄 손실을
  감수해야 하므로, 안전한 분산과 고수익 집중 사이의 선택이 생긴다.
- **난이도의 자연스러운 상승**: 라운드가 지날수록 신청 규모와 밀도가
  커져도, 단순히 확률을 올리는 방식보다 시장이 성장하고 복잡해지는
  감각을 전달할 수 있다.
- **도시의 게임적 역할**: 3D 도시는 장식이 아니라 지역 경제와 위험을
  읽는 게임판이 된다. 물류지구, 상권, 주거지, 기술단지 등은 서로 다른
  고객 구성과 위험 신호를 전달할 수 있다.
- **콘텐츠 확장성**: 새 스테이지에는 지도 데이터와 수요·뉴스 설정을
  추가하는 방식으로 새 지역과 이야기를 만들 수 있다.

## 비목표

- 이 작업에서 실제 지리 지도, 길찾기, 자유로운 건물 내부 탐색을 만들지
  않는다.
- 모든 고객 간 관계를 선으로 그리는 네트워크 시각화는 만들지 않는다.
  축소 상태에서는 집계된 지역·업종 연결만 표현한다.
- 3D 건물마다 고유 모델을 수작업 제작하지 않는다. 건물 유형과 결정적
  변형 규칙을 우선한다.
- 승인 슬롯, 신규 상품, 대출 금리 공식의 전면 재설계는 이 선행 작업의
  범위 밖이다.

## 설계 원칙

- **논리와 렌더링 분리**: 시장 위험과 지도 관계는 순수 TypeScript 데이터로
  정의한다. Three.js와 React는 그 데이터를 표시할 뿐이다.
- **결정성 유지**: 시드와 행동이 같으면 동일한 지역 배치, 고객 생성,
  부도 결과가 나온다.
- **정보의 단계적 공개**: 축소 화면은 지역 집계, 중간 화면은 업종·건물
  클러스터, 확대 화면은 개별 고객을 보여 준다.
- **공정한 리스크**: 부도 전이의 대상과 현재 압박은 승인 전부터 태그,
  경보, 노출 요약으로 알 수 있어야 한다.
- **성능 우선**: 화면 밖의 개별 노드와 연결선은 렌더링하지 않는다. 반복
  건물은 인스턴싱을 사용하고, 확대 수준에 맞춰 디테일을 줄인다.

## 단계별 작업

### 1. 지도 도메인 모델 정의

- `MarketDistrict`, `MarketLot`, `MarketMapNode`, `MarketMapEdge` 타입을
  추가한다.
- 각 지역에 업종 성격, 지역명, 위험 태그, 지도 좌표, 건물 유형 분포를
  정의한다.
- 고객·예금자·상품·자금조달처가 `locationId` 또는 지역 ID를 참조하도록
  확장한다.
- 현재의 고정 위치 배열을 스테이지별 지도 배치 데이터로 대체한다.
- 위치를 찾고 빈 필지를 배정하는 순수 유틸리티와 테스트를 만든다.

완료 기준: 새 스테이지가 코드 분기 없이 지도 데이터만으로 여러 지역과
노드 배치를 정의할 수 있다.

### 2. 공유 카메라와 지도 렌더링 경계 만들기

- `MarketGameView`에서 지도 관련 렌더링을 전용 `map/` 컴포넌트와 모듈로
  분리한다.
- 2D 오버레이와 Three.js 도시가 같은 월드 좌표와 카메라 변환을 사용하게
  한다.
- 현재의 픽셀 단위 동기화 패닝을 월드 좌표 기반 카메라 상태로 교체한다.
- 지도 경계, 최소·최대 줌, 초기 카메라 위치, 접근성 있는 줌 조작을
  정의한다.

완료 기준: 특정 지역·필지에 배정된 고객 노드가 카메라 이동과 확대/축소
중에도 대응하는 3D 건물 또는 지역 위에 정확히 남는다.

### 3. 대규모 노드 표현과 LOD 구현

- 축소 단계에서는 지역별 대출 잔액, 위험 경보, 대기 신청 수를 집계해
  표시한다.
- 중간 단계에서는 업종 클러스터와 핵심 시설을 표시한다.
- 확대 단계에서만 개별 고객 카드와 선택 가능한 상호작용을 렌더링한다.
- 화면 밖 노드와 연결선은 컬링하고, 연결선은 개별 대출선 대신 집계된
  `은행 → 지역/업종` 흐름을 기본으로 한다.
- 선택한 고객 또는 최근 이벤트만 개별 연결·애니메이션을 보여 준다.

완료 기준: 수백 개의 논리 노드를 가진 스테이지에서도 기본 화면은 읽을 수
있고, 동시에 포커스 가능한 DOM 노드 수가 제한된다.

### 4. 3D 도시를 지역 데이터에 연결

- 지역 성격에 맞는 건물 템플릿을 정의한다. 예: 상권, 물류·배달 허브,
  주거지, 기술단지, 저신용 밀집 지역, 공원·공공시설.
- 스테이지 시드와 지역 데이터를 사용해 결정적으로 건물 변형을 생성한다.
- 반복 건물은 `InstancedMesh` 또는 병합 지오메트리로 렌더링한다.
- 멀리 있는 건물은 단순 형태로, 가까운 주요 건물만 창문·간판·외곽선 등
  상세 표현을 사용한다.
- 현재의 대출 건수 기반 색칠 진행도는 지역 성장·위험 상태를 읽을 수 있는
  표현으로 재정의한다.

완료 기준: 서로 다른 스테이지 또는 시드가 명확히 다른 도시 형태를 보이며,
지역의 업종·위험 정보가 건물과 지도 표시에 반영된다.

### 5. 업종·지역 상관 리스크 추가

- 월드에 업종·지역별 스트레스 상태를 추가한다.
- 부도 발생 후 같은 노출군의 미상환 대출과 새 신청에 제한된 위험 압박을
  적용하고, 라운드 경과에 따라 감소시킨다.
- 같은 날 만기 대출의 계산 순서가 결과를 바꾸지 않도록, 그날의 부도는
  스냅샷 위험으로 계산하고 스트레스는 이후 시점부터 적용한다.
- 뉴스, 고객 카드, 지역 요약에 현재 경보와 노출을 표시한다.
- reducer의 결정성·순수성·위험 상한·감쇠·저장 복원에 대한 테스트를
  추가한다.

완료 기준: 플레이어가 사전에 볼 수 있는 업종·지역 정보로 연쇄 위험을
예측할 수 있고, 동일 시드와 행동은 동일 결과를 낸다.

### 6. 라운드별 시장 성장 추가

- 스테이지 설정에 `rounds` 또는 `demandCurve`를 정의한다.
- 라운드별로 신청 건수, 금액, 상환 기간, 업종 집중도, 지역별 수요를
  조정한다.
- 현재 일자 기반 생성 로직은 유지하되, 일자가 어떤 라운드에 속하는지
  계산하는 순수 함수를 추가한다.
- 라운드 전환 시 시장 브리핑과 지도 변화로 증가한 압박을 설명한다.
- 자동 대출 상품이 대기 고객을 한 번에 모두 승인할 수 있으므로, 승인
  규모와 지역·업종 집중, 승인 후 예상 유동성 영향을 사전에 읽을 수 있게
  한다. 자동·수동 승인 처리량 자체는 제한하지 않는다.

완료 기준: 시간이 흐를수록 단순히 수치만 커지는 것이 아니라, 포트폴리오
집중과 유동성 판단이 더 중요해진다.

## 주요 기술 리스크와 대응

| 리스크                                                           | 대응                                                                         |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| 많은 DOM 노드와 SVG 연결선이 화면을 가리고 느려짐                | LOD, 뷰포트 컬링, 집계 연결선, 선택 대상만 상세 렌더링                       |
| 3D 건물마다 별도 geometry·material·외곽선을 만들면 GPU 부담 증가 | 건물 템플릿 인스턴싱, 거리별 상세도, 그림자 범위 제한                        |
| 2D 노드와 3D 도시가 어긋남                                       | 하나의 지도 좌표와 카메라 변환을 두 렌더러가 공유                            |
| 부도 전이가 숨은 RNG처럼 느껴짐                                  | 위험 태그, 지역 경보, 노출 요약, 영향 상한과 감쇠를 사전 표시                |
| 저장된 진행 데이터를 바꿔 기존 세션이 깨짐                       | 저장 마이그레이션을 추가하고, 누락된 지도·스트레스 값에는 안전한 기본값 적용 |
| 기존 대화 중심 루프가 지도 관리에 묻힘                           | 개별 고객의 대화와 심사는 확대·선택 상태에서 계속 핵심 행동으로 유지         |

## 검증 계획

- 순수 지도 배치, 지역별 집계, 스트레스 감쇠, 라운드 계산은 구현 파일과
  같은 위치에 Vitest 테스트를 둔다.
- `pnpm typecheck`, `pnpm --filter @banker-simulation/web lint`,
  `pnpm --filter @banker-simulation/web test`를 실행한다.
- 저사양 화면과 모바일 크기에서 확대·축소, 선택, 키보드 포커스, 모션 감소
  설정을 수동 확인한다.
- 논리 노드 수와 실제 DOM/SVG/Three.js draw call 수를 분리해 측정하고,
  스테이지별 성능 예산을 정한다.

## 확정된 선행 결정

1. 첫 대형 지도는 기존 1·2단계를 유지하고 신규 3단계로 추가한다.
2. 지도 예산은 지역·필지·정적 노드를 합쳐 약 300개로 하고, 상세 고객
   표시는 데스크톱 24개·모바일 12개로 제한한다. 1,000개 논리 노드
   스트레스 검증을 별도로 둔다.
3. 지역은 시각 구획뿐 아니라 위험·수요·뉴스·스트레스를 갖는 게임 규칙
   단위다. 은행 재무는 전체 지도에서 하나로 유지한다.
4. 3D 도시는 여러 지역이 이어진 하나의 전체 도시로 만들고 LOD를 적용한다.
5. 수동 심사와 자동 상품 모두 승인 처리량 제한을 두지 않는다. 자동 상품의
   일괄 승인은 예상 규모와 집중 노출을 읽고 통제해야 하는 유동성·상관
   리스크로 취급한다.
6. 지도 기반부터 상관 리스크와 라운드 성장까지 1~6단계 전체를 구현한다.
